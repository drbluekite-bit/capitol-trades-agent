<?xml version="1.0" encoding="utf-8"?>
<!-- Backups are disabled (android:allowBackup="false"); this file is kept only
     as a safe-guard in case backup is ever re-enabled, and excludes everything. -->
<full-backup-content>
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
